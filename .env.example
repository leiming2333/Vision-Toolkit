# ============================================================
# Vision Toolkit 环境变量示例 —— 至少配置一个 provider 的 API Key
# 使用方法: 复制为 .env 并填入密钥, 或直接在系统环境变量中设置
# ============================================================

# ---- OpenAI (GPT-4o 视觉 + DALL-E 3 生图 + 文本 embedding) ----
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_VISION_MODEL=gpt-4o
OPENAI_IMAGE_MODEL=dall-e-3
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ---- 通义千问 / DashScope (Qwen-VL 视觉 + 万相生图 + 多模态 embedding) ----
DASHSCOPE_API_KEY=
QWEN_VL_MODEL=qwen-vl-plus
QWEN_IMAGE_MODEL=wanx2.1-t2i-turbo
QWEN_EMBEDDING_MODEL=multimodal-embedding-one-peace-v1

# ---- Google Gemini (Gemini 2.0 Flash 视觉 + Imagen 3 生图 + 文本 embedding) ----
GEMINI_API_KEY=
GEMINI_VISION_MODEL=gemini-2.0-flash
GEMINI_IMAGE_MODEL=imagen-3.0-generate-002
GEMINI_EMBEDDING_MODEL=text-embedding-004

# ---- Anthropic Claude (视觉分析: OCR / 检测 / 描述 / 问答; 不支持生图和 embedding) ----
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_VISION_MODEL=claude-sonnet-4-20250514
ANTHROPIC_API_VERSION=2023-06-01

# ============================================================
# Skill 独立配置(可选)
# ============================================================
# 默认情况下, Skill 脚本与 MCP server 共用上面的环境变量。
# 若希望 Skill 用不同的 API Key / base_url / model, 可设置带 SKILL_ 前缀的变量:
#   Skill 运行时优先读 SKILL_<name>, 未设置则回退到 <name>。
# MCP server 不读 SKILL_* 变量, 因此 MCP 行为不受影响。
#
# 示例: Skill 用单独的 OpenAI 兼容端点和 Key
# SKILL_OPENAI_API_KEY=sk-skill-...
# SKILL_OPENAI_BASE_URL=https://my-proxy.example.com/v1
# SKILL_OPENAI_IMAGE_MODEL=dall-e-3
# SKILL_OPENAI_VISION_MODEL=gpt-4o
#
# 示例: Skill 用单独的通义 Key
# SKILL_DASHSCOPE_API_KEY=sk-skill-qwen-...
# SKILL_QWEN_IMAGE_MODEL=wanx2.1-t2i-turbo
#
# 示例: Skill 用单独的 Gemini Key
# SKILL_GEMINI_API_KEY=...
